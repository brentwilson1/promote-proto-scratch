# promote-proto-scratch

**SCRATCH / THROWAWAY.** Toy repo used to answer one question:

> Can a "promote" workflow drive a gated "publish" workflow using only the
> default `GITHUB_TOKEN`, by calling publish via `workflow_call` instead of
> relying on a tag push to re-trigger it?

Contains nothing but two toy workflows. No production code. Safe to delete.
